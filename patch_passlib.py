import sys, os, passlib; passlib_path = os.path.dirname(passlib.__file__); bcrypt_path = os.path.join(passlib_path, "handlers", "bcrypt.py"); content = open(bcrypt_path, "r").read(); content = content.replace("version = _bcrypt.__about__.__version__", "try:\n                version = _bcrypt.__about__.__version__\n            except AttributeError:\n                version = _bcrypt.__version__"); open(bcrypt_path, "w").write(content); print("Patch applied")
